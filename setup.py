# setup.py
from setuptools import setup

setup(
    name="inst_loader",
    version="0.1",
    py_modules=["inst_loader"],  # যদি আপনার ফাইল inst_loader.py হয়, তাহলে এটিই ব্যবহার করুন
    install_requires=[],  # অন্য কোনো ডিপেনডেন্সি থাকলে তা এখানে লিখুন
)
